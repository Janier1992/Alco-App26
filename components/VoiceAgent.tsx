
import React from 'react';

// DEPRECATED: This component is replaced by the Voice Mode in AgentHub.tsx
// It previously used the Node.js SDK which is not compatible with the browser.
// Keeping file for reference but disabling logic to fix build.

const VoiceAgent: React.FC = () => {
    return null;
};

export default VoiceAgent;
